package NewJavaTest.LatestCoreJavaPractise;

public class PackageDemo {

	public static void main(String[] args) {
		
		// We can use method of one class into another class if they are coming under the same package through inheritance and creating objects 
		// In the current case, we have a package named "NewJavaTest.LatestCoreJavaPractise" and under this we have all the classes and then we are using methods of one class into another
		// But if we have a java class in another package then we can't use their methods one into another and if we want to use their methods then we need to import their package
		// For importing package, we can use "import package.classname"
		//I have created an package "testPackag" and import the package to use method of another class

	}

}
